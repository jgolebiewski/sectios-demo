import { TopBarWrapper } from './TopBar.styled';

export const TopBar = (): JSX.Element => {
    return (
        <TopBarWrapper>
            <h1>Vacation reports</h1>
        </TopBarWrapper>
    );
};
