import styled from 'styled-components';

export const AppWrapper = styled.section`
    width: 100%;
    min-height: 100vh;
    margin: 0;
    padding: 0;

    display: flex;
    flex-direction: column;
`;
